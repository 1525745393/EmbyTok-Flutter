# 纯净模式按钮区可拖动 - Verification Checklist

## Task 1: 可拖拽容器
- [ ] `_buildCleanModeRightActions()` 返回的按钮区使用动态 `Positioned(left/top)` 定位
- [ ] 新增的 `_DraggableCleanActions` 内部维护 `_offset` 状态
- [ ] 初始位置在右侧垂直居中
- [ ] 非纯净模式（完整按钮区）的行为不变

## Task 2: 点击与拖拽区分
- [ ] 轻触按钮（位移 < 10px）正常触发按钮点击行为
- [ ] 按下并拖动（位移 > 10px）触发拖拽
- [ ] 拖拽结束后不触发按钮点击
- [ ] 拖拽过程中内部按钮的点击被屏蔽（`IgnorePointer`）

## Task 3: 视觉反馈
- [ ] 拖拽时按钮区放大（1.1x）
- [ ] 拖拽时按钮区增加阴影
- [ ] 松手后按钮区恢复原始大小和阴影
- [ ] 过渡动画平滑无卡顿

## Task 4: 边界限制
- [ ] 按钮区不能拖出视频播放区域
- [ ] 可以拖动到视频区域的任意角落
- [ ] 边界处停止移动，不卡住

## Task 5: 状态切换与代码质量
- [ ] `isAutoPlay` 切换为 true 后，按钮从默认位置开始
- [ ] 代码无语法错误
- [ ] 修改范围最小化
- [ ] 不影响其他已有功能
