# Tasks

- [x] Task 1: 横屏视频全屏沉浸播放
  - [x] 在 `video_page_item.dart` 添加 `_isFullscreen` 状态（State 局部状态）
  - [x] 右侧操作列新增"全屏"按钮（Icons.fullscreen），点击切换横屏/竖屏
  - [x] 横屏模式调用 `SystemChrome.setPreferredOrientations([DeviceOrientation.landscapeLeft])`
  - [x] 竖屏模式恢复 `SystemChrome.setPreferredOrientations([DeviceOrientation.portraitUp])`
  - [x] 横屏模式隐藏顶部工具栏（`toolbarVisibilityProvider` 设为 false）和底部导航栏
  - [x] 横屏模式视频容器改为黑色背景居中布局
- [x] Task 2: 视频画面填充策略动态调整
  - [x] 在 `video_player_widget.dart` 根据 `widget.item.isLandscape` 选择 BoxFit
  - [x] 竖屏视频：`BoxFit.cover`（填满）
  - [x] 横屏视频：`BoxFit.contain`（完整显示，上下黑边）
- [x] Task 3: 接入 VideoControls 播放控制层
  - [x] 在 `video_page_item.dart` 的 Stack 中叠加 `VideoControls`
  - [x] 位置：底部，距底部 kBottomNavHeight（竖屏）/0（横屏）
  - [x] 样式：半透明黑色背景渐变（VideoControls 自带）
  - [x] 显示内容：当前时间 / 总时长 / 进度条 / 倍速按钮
  - [x] 进度条支持点击跳转（VideoControls 内部 Slider 实现）
- [x] Task 4: TikTok 风格底部细线进度条
  - [x] 在 `video_page_item.dart` 添加 `_ThinProgressBar` 组件
  - [x] 高度 2px，宽度 = 视频画面宽度
  - [x] 颜色：`primaryPink`（#E91E63），背景半透明黑色（30% alpha）
  - [x] 始终可见（不受控制层隐藏影响）
  - [x] 监听 VideoPlayerController 的 position 变化更新进度
- [x] Task 5: 中央播放/暂停按钮
  - [x] 在 `video_page_item.dart` 添加 `_buildCenterPlayButton` 方法
  - [x] 暂停时显示：半透明圆形播放图标（直径 72dp，alpha 0.6）
  - [x] 播放时隐藏
  - [x] 点击中央按钮调用 `controller.play()`
- [x] Task 6: 控制层自动隐藏逻辑
  - [x] 在 `video_page_item.dart` 添加 `_controlsVisible` 状态和 `_controlsHideTimer`
  - [x] 控制层显示时启动 3 秒计时器，到期淡出隐藏（300ms）
  - [x] 单击画面切换控制层显示/隐藏（修改 `gesture_overlay.dart` 的 `_onSingleTap`）
  - [x] 控制层隐藏时，底部细线进度条和右侧操作按钮仍保留
  - [x] 显示控制层时重置 3 秒计时器

# Task Dependencies
- [Task 2] 依赖 [Task 1]（需要全屏状态判断）
- [Task 3] 独立，可并行
- [Task 4] 独立，可并行
- [Task 5] 独立，可并行
- [Task 6] 依赖 [Task 3]（控制层存在才能隐藏）
- [Task 1] 独立，最先实施
